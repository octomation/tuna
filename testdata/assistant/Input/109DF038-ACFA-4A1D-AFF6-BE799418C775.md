The API endpoint accepts POST requests with a JSON payload containing the following required fields: "user_id" (string), "timestamp" (ISO 8601 format), and "event_type" (enum: "click", "view", "purchase"). Optional parameters include "metadata" (object) and "session_id" (string). Rate limiting is set to 100 requests per minute per API key.
